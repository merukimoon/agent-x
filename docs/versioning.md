# Versioning

This project intends to follow Semantic Versioning (SemVer) once it publishes releases (**TODO** confirm).

## Pre-1.0 policy (recommended)

Until `1.0.0`:

- Minor versions (`0.X.0`) may include breaking changes
- Patch versions (`0.X.Y`) should be backwards compatible within that minor line

## Compatibility notes (**TODO**)

Define what “compatibility” means for this project:

- Public API surface (types/functions/classes)
- Prompt formats and configuration schemas
- On-disk formats (state, logs) if any

## Release checklist (recommended)

- Update `CHANGELOG.md`
- Ensure docs match behavior
- Tag release (`vX.Y.Z`)
- Publish artifacts (registry, binaries, etc.) (**TODO**)

