name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch: {}

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Repository sanity
        run: |
          echo "CI placeholder for agentsquad-framework"
          echo "TODO: add language-specific build/test/lint steps."

