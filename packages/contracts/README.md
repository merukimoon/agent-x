# Contracts

Canonical package for AgenticX contract types (run artifacts, step lifecycle, overrides).

This package is the source of truth for enforceable contracts and is consumed by the CLI and core libraries. It is private, versioned with the monorepo, and replaces ad hoc contract definitions that previously lived under `packages/core/src/contracts`.
